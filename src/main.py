"""
Main Application (Integration and Testing)

Task:
1. Integrate all modules into the GUI:
   - Import the following:
       from gui import create_gui
       from core_functions import resize_image, crop_image
       from filters import apply_filter
       from tools import draw_on_image, add_text
2. Ensure proper communication between the GUI and the backend functions.
3. Test the complete application to ensure all features work together seamlessly.

Steps:
- Set up the main execution flow to initialize the GUI and link all features.
- Test each feature independently and as part of the full application.
- Debug any integration issues and refine the application for usability.
"""
